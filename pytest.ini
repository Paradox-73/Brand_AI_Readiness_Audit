[pytest]
testpaths = tests
markers =
    mutation: breaks one property of a clean fixture and asserts the audit
              notices that property and nothing else. Slow - a full crawl and
              audit per case. Deselect with -m "not mutation".
